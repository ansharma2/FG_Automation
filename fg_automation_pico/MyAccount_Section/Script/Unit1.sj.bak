function xx(){
//let arr = [];

//$('.mat-tab-body-content .ng-star-inserted').each(function(){
   // if ($(this).find('span.tripType').length > 0)
    {
        const str = $(this).find('span.tripType').html().toLowerCase().trim();
        switch (str)
        {
            case 'refunded' :
                console.log('Refunded');
                break;
            case 'past journey' :
                console.log('Past Journey');
                break;
            case 'upcoming journey' :
                console.log('Upcoming Journey');
                break;
            default:
                console.log('Fail');
        }
  }
});
//console.log(arr);
}